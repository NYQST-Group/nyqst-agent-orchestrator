name: External Schema Publisher

on:
  push:
    branches: [main]
    paths:
      - 'src/intelli/schemas/**'
      - 'ui/src/types/**'
      - 'packages/ui-library/types/**'

defaults:
  run:
    shell: bash

jobs:
  publish-catalog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate Contract Drift
        run: |
          echo "Validating Pydantic to TypeScript coherence..."
          echo "::error::Contract validation step is currently a mock and must be implemented before enabling this workflow."
          exit 1
          
      - name: Publish to External Catalog
        run: |
          echo "Triggering external catalog publish via repository dispatch..."
          echo "::warning::Catalog refresh step is currently a mock. Needs implementation via repository dispatch."
